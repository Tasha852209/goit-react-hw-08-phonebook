import styled from 'styled-components';

export const StyledApp = styled.div`
  min-width: 320px;
  max-width: 40%;
  background-color: rgba(255, 255, 255, 0.85);
  border: 1px solid transparent;
  border-radius: 30px;
  box-shadow: 3px 2px 30px 0 rgba(0, 0, 0, 0.6);
  height: 100vh;
  padding: 15px;

  margin: 0 auto;
`;
